# StillAlive 小白用户体验报告

日期：2026-08-25  
测试地址：http://127.0.0.1:8000

## 测试范围

本次以“我想在死后给朋友留下一封信”的普通用户视角完成了两轮体验：

1. 以管理员身份配置朋友、识别问题、专属验证问题和遗产内容。
2. 退出管理员语境，从朋友视角完成识别、确认、验证和下载。

除使用 Python 从本地 `start_local.py` 读取管理员登录配置并生成 TOTP 外，所有项目操作均通过浏览器 UI 完成，没有修改应用代码或数据库。为上传测试信，额外生成了一个临时明文文本文件。

## 第一轮：管理员配置遗产

### 登录

- 管理员登录页可以正常使用用户名、密码和动态验证码登录。
- 登录成功后进入总览页，导航结构清晰。
- Dashboard 能看到人物、问题、Identity Integrity、Public Delivery 和 Sealed Release 状态。

### 朋友数据

我完成了以下操作：

- 创建朋友：`小白的朋友A`。
- 打开详情页查看朋友资料、内部 UUID、状态、识别答案、验证问题和遗产内容。
- 修改状态：另建 `体验用朋友B`，然后将其归档；归档后它不再参与当前识别。
- 未修改已有的其他人物资料，避免误碰已有数据。

结论：创建、查看和状态修改可以完成，但并不是完整 CRUD：

| 操作 | 结果 |
|---|---|
| 创建 | 通过 |
| 查看 | 通过 |
| 修改显示名称/资料 | 没有明显入口 |
| 修改状态 | 通过，可 active/disabled/archived |
| 删除 | 没有删除入口，只能归档 |

### 识别问题与身份模型

通过“问题”页面创建了 3 道 L1 低隐私问题：

- 我们是否一起参加过线下活动？
- 我们是否一起玩过同一款游戏？
- 我们是否曾经在工作上认识？

随后在朋友 A 详情页为三道题填写了 `是 / confidence 1.0`。

Identity Integrity 页面能够显示 Pair、混淆率、Strong Discriminator、Cluster 和风险解释。但当前已有其他 active 人物没有填写这些问题的 Trait，导致：

- Identity Integrity：`BLOCKING`
- Blocking Pair：3
- Worst Confusion Rate：95.60%
- Sealed Release：不能创建

这说明完整性检查确实能阻止不可靠的封存，但对普通用户而言，问题在于页面只告诉我“哪里红了”，没有把“下一步要给哪些人补哪些答案”做成足够直观的待办列表。

### 验证问题

为朋友 A 创建了：

> 我们第一次一起旅行的城市是？

可接受答案设置为“杭州”和“Hangzhou”。保存后页面只显示“已配置 2 个可接受答案”，没有显示答案原文，符合安全预期。

### 遗产内容

上传了一封 434 bytes 的文本信，展示名为“写给朋友们的信”。上传后后台显示内容类型、大小、Asset UUID 和 active 状态。

我测试了：

- 上传：通过。
- 后台查看：通过。
- 停用 Asset：通过。
- 重新启用 Asset：通过。
- 修改展示名/替换文件：没有明显入口。
- 删除 Asset：没有入口，只能停用。

因此遗产内容同样是“创建、查看、启停”可用，但不是完整 CRUD。

### Sealed Release

封存版本页能显示 Gate Checklist。当前明确失败项包括：

- Identity Integrity 仍为 BLOCKING。
- 有 active 人物没有 Verification Challenge。
- Recovery Key 尚未配置。

页面还直接显示了 `data\\vault`、本地 requirements 路径、`missing=1` 等工程信息。对普通用户而言，这些信息不够友好，也没有形成“点击即可修复”的操作路径。

本次没有宣称 Sealed Release 完成；真实 Docker OCI `build/save/load` 仍受本机没有 Docker daemon、远端 CI 结果不可读取的环境限制。

## 第二轮：朋友前台领取遗产

我打开了新的匿名前台会话，没有使用后台身份：

1. 点击“开始识别”。
2. 连续回答 3 道问题“是”。
3. 页面给出唯一猜测：“我猜你是：小白的朋友A”。
4. 点击“是我”。
5. 看到专属验证问题。
6. 输入“杭州”。
7. 页面显示“身份验证通过”。
8. 看到“写给朋友们的信 · 434 bytes”。
9. 点击“下载”，浏览器成功触发下载事件。

前台主流程通过，且没有暴露全量人物、候选评分、验证答案或密钥。页面文案也没有要求等待 Heartbeat、倒计时或 RELEASED 状态。

## 作为小白用户的主要问题

### P0：产品语义与“死后交付”不一致

当前版本部署启动后立即开放交付。也就是说，只要朋友知道入口并猜中/验证成功，现在就能拿到内容，并不是“我死后才开放”。这符合当前 SPEC，却不符合“死后留信”的直觉期待。

建议首次配置时明确展示：

> 当前版本是“立即可用的定向交付”，不是“死后自动开放”。如果你需要延迟到未来触发，需要另行启用生命周期策略。

这条提醒应出现在首次设置、Dashboard 和 Release 页面，而不是只写在工程规范里。

### P0：首次使用没有向导

小白不知道应该先建什么。建议提供一个强制但可跳过的首次配置向导：

```text
创建朋友
→ 创建低隐私识别问题
→ 为每个 active 朋友填写答案
→ 创建专属验证问题
→ 上传遗产内容
→ 用前台试跑一次
→ 配置 Recovery Key
→ 检查并创建 Sealed Release
```

每一步都应显示完成数量和“去处理”按钮。

### P0：缺少真正的编辑和删除

朋友和 Asset 页面都没有清晰的编辑/删除能力。建议：

- 朋友支持修改显示名、头像、备注。
- Asset 支持修改展示名、替换版本、查看版本历史。
- 默认删除应是“归档/停用”，并明确说明数据是否保留。
- 对真正永久删除提供二次确认，并说明关联的 Challenge、Grant、Audit 会如何处理。
- Challenge 也应支持编辑、停用、删除已有答案 digest。

### P1：后台中英文混杂

普通用户会遇到 `Identity model incomplete`、`Verification Challenge`、`Assets`、`Facet tag`、`Active Person` 等词。建议提供完整中文：

- Identity model incomplete → 身份模型未完成
- Verification Challenge → 专属验证问题
- Assets → 遗产内容
- Sealed Release → 离线封存包
- Facet tag → 区分维度（可选）

内部枚举如 `L1_RELATION`、`BLOCKING` 可以保留，但应配中文解释和悬浮提示。

### P1：身份完整性问题缺少行动导向

新建朋友后页面显示“最相近人物”和风险 Pair，但向导有时直接显示 UUID，普通用户不知道那是谁。应始终显示姓名，并将问题改成待办：

> 朋友 A 与朋友 B 容易混淆。请先为 B 补充“是否一起参加过线下活动？”的答案。

同时应提供“只查看我新建的朋友”“只看缺少答案的人”“只看会阻止封存的人”等筛选。

### P1：页面过长，创建表单一直留在页面底部

朋友详情页包含状态、全部 Trait、Challenge 创建表单和 Asset 上传表单，滚动很长。Challenge 创建成功后，空白创建表单仍然保留，容易误以为刚刚保存失败或重复创建。

建议将详情页改为 Tab 或步骤卡片，并在成功后折叠创建表单，改为“新增另一个验证问题”。

### P1：遗产内容缺少“包”的概念

用户说的是“给朋友留一封信”，页面却只显示 `Asset` 和 UUID。建议改成更接近用户心智的结构：

```text
朋友 A
└─ 遗产内容
   ├─ 写给朋友们的信
   ├─ 图片包
   └─ 重要文件
```

同时把“遗产内容”和“离线封存包”明确区分：前者是朋友最终拿到的东西，后者是管理员用于灾难恢复的系统备份。

### P1：前台还可以更安心

当前前台很简洁，问题按钮和验证流程容易理解，这是优点。但还可以增加：

- 当前处于第几步的非泄露式进度提示。
- “我不确定”与“大概是”的解释。
- 验证失败后的下一步说明和剩余尝试次数。
- 下载链接有效期说明。
- 下载完成后显示“此链接已使用”或“重新生成下载链接”。

本次观察到下载按钮再次点击仍会触发新的下载事件，说明每次点击都可能重新生成一个新的 Grant。若产品希望严格“一次性领取”，应在 session 或 Asset 层限制重复发放，或明确文案为“每个下载链接只能使用一次”。

### P1：封存失败页面要面向人，而不是面向开发者

Gate Checklist 很完整，但 `missing=1`、本地路径和 runtime artifact 等信息不适合普通管理员。建议每一项提供：

- 中文状态；
- 为什么重要；
- 如何修复；
- “立即处理”按钮；
- 修复后自动重新检查。

## 验收结论

| 场景 | 结论 |
|---|---|
| 管理员登录 | 通过 |
| 创建/查看朋友 | 通过 |
| 修改朋友状态 | 通过 |
| 朋友资料完整编辑 | 不完整 |
| 朋友删除 | 不支持，仅归档 |
| 创建/查看遗产内容 | 通过 |
| 启停遗产内容 | 通过 |
| 遗产内容编辑/删除 | 不完整/不支持 |
| Discovery | 通过 |
| Confirmation | 通过 |
| Verification | 通过 |
| Download | 通过，浏览器成功触发下载 |
| Identity Integrity | 检测到 BLOCKING，未通过 |
| Sealed Release | 被正确阻止，未创建 |
| Docker OCI 离线恢复 | 本次无法验证 |

总体判断：StillAlive 的核心“识别朋友并交付专属内容”链路已经可以被普通用户走通；但距离“普通人可以安心配置、死后长期托付”的产品体验，还需要补上首次配置向导、真正的 CRUD、身份完整性待办化、封存恢复引导，以及对“立即交付”和“死后触发”差异的明确产品说明。

## 补充：UI 专项评审

### 已有优点

- 前台采用单一主任务布局，`开始识别`、五级回答按钮、`是我`和`下载`都容易找到。
- 米白背景、深色标题和绿色主按钮形成了统一的视觉语言，整体有“长期保存、安静、私密”的感觉。
- 前台隐私提示始终可见，且没有把候选人物、概率和密钥暴露给访客。
- 管理后台顶部导航固定且分类明确，人物、问题、身份完整性、模拟器、审计和封存版本的入口容易找到。

### 已观察到的 UI 问题

1. “Identity model incomplete” 使用了接近成功提示的浅绿色背景，容易让人以为状态正常。未完成应使用黄色/橙色，BLOCKING 应使用红色，并配合图标、中文解释和下一步按钮。
2. 后台详情页是很长的单页，识别答案、验证问题、遗产内容全部纵向堆叠。建议改成 Tab 或步骤卡片，并增加右侧固定的“完成度/下一步”面板。
3. 保存按钮不够有层级。建议使用一个固定的主操作区，显示“有未保存修改”“已保存”“保存失败”，避免用户滚动到页面底部找按钮。
4. 创建成功后空白表单仍保留在页面上。例如 Challenge 已配置后，页面下面仍显示空的验证问题表单，容易造成重复提交。保存成功后应折叠表单，只显示“新增另一个验证问题”。
5. 页面同时出现中文、英文、内部枚举和 UUID。UUID 对普通管理员几乎没有帮助，应默认隐藏，只提供“复制内部 ID”；`L1_RELATION`、`BLOCKING` 等应有中文标签和帮助说明。
6. Wizard 中出现原始人物 ID，而不是始终显示人物姓名。冲突解决流程必须面向“谁和谁容易混淆”，不能要求用户识别 UUID。
7. `value=-1.0..1.0` 和 `confidence=0..1.0` 是工程字段，不是小白友好的输入方式。建议改成“是 / 大概是 / 不知道 / 大概不是 / 不是”的按钮或单选项，confidence 用“我很确定 / 一般确定 / 不太确定”表达，必要时再展开高级数值。
8. 空状态缺少上下文动作。比如“暂无问题”下面应直接出现“创建第一道识别问题”，并说明为什么必须先创建问题，而不是让用户自行回到导航栏寻找。
9. 人物列表、问题列表和遗产内容列表缺少明显的搜索、筛选、排序和批量操作。数据稍多后，用户很难找到“缺少验证问题的人”或“会阻止封存的人”。
10. 危险操作的视觉区分不够。停用、归档、删除、撤销封存应有明确的危险色、影响范围和二次确认；普通保存不应和危险操作使用相同视觉权重。
11. 审计、封存 Gate 和完整性页面更像开发者诊断页。它们应同时提供“技术详情”和“普通说明”两层视图，默认先告诉用户“现在为什么不能封存、应该做什么”。
12. 前台首页的标题“长期可恢复的定向数字内容交付系统”准确但偏产品/工程语言。建议主标题更像用户任务，例如“找回一份只属于你的信”，副标题再解释 StillAlive 的长期恢复能力。
13. 前台流程没有明显的非泄露式进度反馈，也没有“暂时离开后如何回来”的说明。可以显示“第 2 个问题”或阶段名，但不能显示候选人数和概率。
14. 前台验证完成页把重点写成“识别完成”，用户可能以为任务已经结束。建议改为“你的内容已解锁”，并明确说明下载链接有效期、是否只能下载一次，以及下载后应该做什么。
15. 前台应该提供不依赖颜色的状态表达，保证色觉障碍用户也能分辨 PASS、WARNING、BLOCKING；所有按钮、输入框、提示和键盘流程需要做可访问性验收。

## 多模态最终验证

这是一个很重要的产品扩展。共享回忆不一定是文字，也可能是声音、照片、视频或一组物件。最终验证应支持多模态，但不能简单地把“上传任何文件”当成自动验证，否则会引入隐私泄露、恶意文件和无法判定的问题。

### 建议的挑战类型

第一版可以先支持以下四类，按实现风险从低到高逐步增加：

1. **媒体提示 + 文字回答**：展示一张照片、播放一段音频或视频，然后让访客回答文字问题。验证仍然使用现有的 normalization + HMAC，最容易保持确定性和安全边界。
2. **图像选择题**：展示多张图片，让访客选择“我们第一次见面的地方是哪一个”。服务端只保存正确选项的 digest，不把答案字段暴露给前端。
3. **音频/视频提示 + 选择或文字回答**：播放共享回忆后，要求访客选择或输入答案。必须提供字幕、文字描述和无法播放时的替代验证问题。
4. **访客媒体回答**：允许访客录音、拍照或上传视频作为回答。这类回答不应默认由 AI 自动判定，可以先作为“人工审核型 Verification”，或只支持严格的文件指纹匹配场景。

### 后台配置方式

每个 Verification Challenge 建议包含：

- challenge 类型；
- prompt 文本或说明；
- 一个或多个加密媒体附件；
- 可接受的回答方式；
- 是否允许文本备用回答；
- 最大文件大小、时长和格式限制；
- 失败后的替代 Challenge；
- 媒体保留期限和是否允许管理员预览。

后台 UI 不应把所有字段一次性展开。建议使用“选择验证方式 → 上传/预览媒体 → 设置正确答案或审核方式 → 试运行 → 保存”的向导，并在保存前显示访客实际看到的预览。

### 安全要求

- 验证媒体必须与 Asset 一样加密保存，不能放到静态目录或第三方 CDN。
- 只有 Discovery 已确认的当前 Person session 才能获取对应媒体；不能让访客先枚举照片或视频。
- 上传时限制 MIME、大小、时长和分辨率，进行恶意文件扫描，并清理不必要的 EXIF/GPS 元数据。
- 音频、视频和人像可能属于高度敏感信息，后台上传前应显示隐私提醒和保留策略。
- 不自动把语音、人脸或视频用于生物特征识别；如果未来要做，必须单独设计同意、误判申诉、加密和删除机制。
- 媒体播放应禁止不必要的自动播放，使用安全的 `Content-Disposition` 和 CSP，避免脚本化媒体成为攻击入口。
- 不能播放、听不清或有残障的访客必须能切换到文字描述、字幕或备用 Challenge，不应因为多模态而失去领取机会。

### 不建议的实现

不建议第一版直接使用“让 AI 判断这是不是朋友的声音/脸/回忆”作为自动放行条件。它会带来误判、隐私、生物识别和无法解释的问题，也与当前“答案验证必须确定、不得用模糊语义 AI 自动判对”的安全语义冲突。

## 更多产品优化建议

### 1. 把“遗产内容”和“离线封存包”分成两个用户概念

普通用户想管理的是“朋友最终收到的内容”，管理员还需要管理“服务器坏了以后如何恢复系统”。建议在产品中使用：

- **遗产内容**：信件、照片、视频、文件，是朋友最终领取的对象。
- **离线封存包**：系统、数据库快照、加密 Vault 和恢复材料，是管理员的灾难恢复对象。

两者不要都简称为“包”，否则用户会不知道自己是在上传信，还是在制作备份。

### 2. 为每个朋友提供“交付预览”

管理员应能点击“以朋友视角预览”，进入隔离的测试 session，看到：

- 实际会被问到的问题；
- 最终验证的形式；
- 朋友能看到的内容名称和大小；
- 下载前后的提示。

预览必须使用 fixture 或测试 namespace，不创建真实 Grant，也不污染正式审计。

### 3. 支持多个交付版本和撤回

信件可能需要更新。建议 Asset 支持版本：

- 草稿；
- 已发布；
- 已撤回；
- 历史版本只读。

管理员应能明确看到“朋友现在会拿到哪一版”，而不是只能停用整个 Asset 后重新上传。

### 4. 识别问题模板和维度引导

小白最难的是“不知道该问什么”。可以提供非 AI 的问题模板和维度提示，例如：

- 一起做过什么；
- 第一次认识的场景；
- 共同经历的时间地点；
- 只有你们知道的物件或习惯。

模板只能帮助创作，不能自动把隐私资料生成问题，也不能绕过管理员确认。

### 5. 建立内容安全检查清单

上传遗产内容后，系统应提示：

- 文件是否包含其他人的隐私；
- 图片是否带有 GPS 元数据；
- 视频/音频是否包含不想公开的背景内容；
- 这份内容是否真的绑定到了正确的朋友；
- 是否已经做过一次测试领取。

检查结果应是提醒，不应偷偷修改用户文件。

### 6. Recovery Key 应成为可验证的任务

现在 Recovery Key 页面能创建或轮换，但小白不一定知道创建后要做什么。建议流程变成：

```text
生成 Recovery Key
→ 要求用户下载/抄录并确认已离线保存
→ 用 Recovery Key 做一次本机解封测试
→ 显示“恢复材料可用”
→ 才允许创建 Sealed Release
```

Recovery Key 不应进入普通剪贴板、日志、截图或封存包明文。

### 7. 增加通知和健康状态，但不引入隐含生命周期门禁

可以提醒管理员：完整性过期、有人物没有验证问题、Asset 解密自检失败、Release 即将被撤销或离线副本长期未验证。提醒只用于运维，不得悄悄改变 Public Delivery 是否开放。

### 8. 更清楚的权限与协作模型

未来如果不止一个管理员，建议拆分：

- 内容编辑者；
- 安全审核者；
- 恢复/封存管理员。

上传敏感内容、轮换 Recovery Key、创建 Release 等操作可以要求双人确认，Audit 中显示谁创建、谁复核，但不记录信件原文或验证答案。

### 9. 用普通语言解释“安全边界”

首次登录、创建 Challenge、上传多媒体和导出封存包时，都应告诉用户：

- 猜中朋友不等于验证成功；
- 验证答案不会以原文保存；
- 服务器管理员/数据库泄露不应直接得到信件明文；
- Recovery Key 与封存包不能放在同一个地方；
- 当前版本是否立即可用，是否有未来触发条件。

这样用户才能理解为什么系统有多个步骤，而不会把它们误认为多余的复杂度。

## UI 与产品优化优先级

建议下一轮按以下顺序处理：

1. 首次配置向导、中文化和“下一步”行动入口。
2. 朋友/遗产内容/Challenge 的编辑、归档、删除语义和版本管理。
3. Identity Integrity 的姓名化、待办化和可修复状态。
4. 前台的进度、失败反馈、下载状态和重复 Grant 说明。
5. 多模态验证的“媒体提示 + 文字/选择回答”基础版本。
6. Recovery Key 验证与 Sealed Release 普通用户引导。
7. 多媒体安全扫描、访问控制、无障碍和移动端专项验收。

## 第二轮复测：首轮 P0/P1 优化后的结果

复测日期：2026-08-25。此次只通过浏览器 UI 验证，没有修改代码或直接修改数据库。

### 已生效的改进

- `/admin/setup` 已可用，首次配置路线、进度数字、下一步链接和“立即交付而不是去世后交付”的说明都已出现。
- Dashboard 统计已经使用真实数据，主要字段补充了中文解释和可执行入口。
- 人物列表现在显示识别答案完成数、启用的验证问题数和启用的遗产内容数。
- 人物支持进入“编辑人物”，实际可以修改显示名称和状态；本次用测试人物完成了改名再改回，保存生效。
- 遗产内容显示名称现在可以直接编辑并保存。
- Verification Challenge 已支持停用/启用。
- 前台增加了“识别 → 确认 → 验证 → 交付”的流程进度，以及“服务启动后立即提供内容”的交付说明。

这些改动让系统从“能跑”向“能配置”前进了一步，尤其是首次配置向导对新用户帮助明显。

### 问题 1：人物仍然不能删除

复测人物列表和人物详情，没有发现“删除”按钮或删除入口。当前页面明确提供的是：

- 启用；
- 停用；
- 归档；
- 编辑名称和状态。

因此，用户“改变主意，不想给这个人任何东西”的需求仍然只能通过归档和停用间接实现，数据本身仍保留。

这里建议不要只增加一个危险的“永久删除”按钮，而是明确拆成三种操作：

1. **撤销交付**：立即停止该朋友的 Discovery、Verification 和 Download，保留内容与审计，适合“我改变主意了”。
2. **归档人物**：从日常列表和识别中隐藏，数据可恢复，适合暂时不使用。
3. **永久删除**：删除人物、Challenge、Trait、Grant、关联 Asset 和元数据；必须显示影响范围、输入人物名称确认，并明确不可恢复。

对于“我不想给他东西”，默认按钮应该叫“撤销交付”，而不是让用户猜“归档”是否足够。归档人物目前还会继续占据后台列表，也容易让人误以为已经彻底删除。

本次没有点击删除，因为当前没有删除入口；此前创建的测试人物仍处于归档状态。

### 问题 2：`value`、`confidence`、`source note` 仍然像工程后台

在朋友详情的识别答案区域，首轮优化后仍然显示：

- `value`；
- `confidence`；
- `source note`；
- `Trait value -1.0 到 1.0，confidence 0 到 1.0`。

首次配置向导告诉用户“去补充答案”，但进入具体表单后并没有解释这些字段为什么存在。对小白来说，页面实际上是在问“请输入三个我不知道含义的数字”。

建议提供普通模式和高级模式：

| 当前字段 | 普通模式文案 | 普通用户操作 |
|---|---|---|
| value | 这个问题对朋友来说是什么答案？ | 是 / 大概是 / 不知道 / 大概不是 / 不是 |
| confidence | 你对这个答案有多确定？ | 很确定 / 大致确定 / 不太确定 |
| source note | 只有管理员能看到的备注 | 可选，例如“来自 2021 年旅行照片” |

高级模式再显示 `-1.0..1.0` 和精确 confidence。每个字段旁应有一句解释：

- 答案用于让系统区分朋友，不会展示给前台访客。
- 确定程度用于降低不可靠记忆对识别结果的影响。
- 备注只帮助管理员回忆来源，不会发送给朋友，也不会参与猜测。

更好的交互是直接把五级答案做成带文字的单选卡片，保存后显示“已回答：是；确定程度：很确定”，而不是让用户回看数字。

### 问题 3：最终验证答错后仍然没有用户可见反馈

复测中提交了错误的最终答案。结果是：

- 页面仍停留在验证表单；
- 验证问题和输入框仍然存在；
- 没有出现“答案不正确”；
- 没有显示剩余尝试次数；
- 没有显示冷却倒计时；
- 页面中的 `status` 区域为空。

后台审计已经记录 `verification.failed`，说明服务端知道失败，但前台没有把这个结果告诉用户。这是一个 P0 级可用性问题，也会让用户误以为按钮没有反应，继而重复提交。

建议错误状态至少显示：

> 这次答案不正确。还可以尝试 4 次。请等待 2 秒后再提交。

并提供：

- 清晰的错误提示，不泄露正确答案；
- 剩余次数和冷却倒计时；
- 提交期间按钮 loading/禁用状态；
- 达到上限后的“本次验证已锁定”页面；
- 锁定后的下一步，例如重新开始识别、联系管理员或使用另一个已配置 Challenge；
- 屏幕阅读器可读的 `role=alert` 或等价错误语义。

### 问题 4：UI 仍然像工程测试站，不像值得托付遗言的产品

这不是“有没有功能”的问题，而是信任感问题。当前 UI 有统一的米白色背景、绿色按钮和圆角卡片，整洁但非常通用；前台标题是“长期可恢复的定向数字内容交付系统”，后台标题是“管理后台”，会让朋友感觉自己进入了一个内部测试工具，而不是一封认真准备的信。

从视觉检查看，主要问题是：

- 标题字号很大，但页面信息层级仍偏像模板页面；
- 大量米白留白和圆角卡片缺少内容个性；
- 后台顶部“管理后台”占据视觉中心，真正重要的“下一步处理什么”反而不突出；
- `身份答案已补齐` 的绿色提示下面仍出现 `Identity model incomplete`，视觉和文字互相矛盾；
- 状态颜色和按钮颜色缺乏一致的语义系统；
- 内部 UUID、英文枚举和工程词汇破坏了完成度；
- 前台下载成功后仍像一个普通表单结果，没有“这是一份专门给你的内容”的情感落点。

建议把 UI 分成两个视觉层次：

**后台：可信的内容管理工具**

- 更清楚的状态徽章：已完成、待处理、风险、已撤销；
- Dashboard 首屏只显示“现在需要我做什么”；
- 复杂字段默认折叠到“高级设置”；
- 使用人物头像/首字母、内容封面和清晰的卡片标题；
- 对删除、撤销交付、轮换密钥等危险操作使用明确的警示样式。

**前台：私人、克制、有记忆感的交付页面**

- 未验证前保持匿名和克制，不泄露人物信息；
- 验证成功后可以展示管理员配置的署名、封面、主题色或一句欢迎语；
- 允许管理员为每个朋友选择“信件 / 相册 / 语音 / 视频 / 文件”内容类型；
- 下载页使用“内容已为你解锁”这类明确文案，而不是只显示“识别完成”；
- 支持一套温和但不俗套的主题模板，让用户可以表达自己的风格；
- 提供手机端预览，因为朋友很可能通过手机打开链接。

用户说“不希望朋友觉得我是一个潦草的人”，本质上是在要求“交付体验可被我设计”。这意味着主题、署名、封面、内容排序、开场文案和下载后的结束语应该成为可配置的内容，而不是硬编码的系统默认页面。

## 这四个问题对应的新增 P0/P1 要求

### P0-UX-01：可撤销交付和永久删除

每个人物必须有“撤销交付”“归档”“永久删除”三个明确动作，显示后果和恢复能力。撤销交付不应删除审计；永久删除必须处理关联 Asset、Challenge、Grant 和密文文件，不能只删除人物行。

### P0-UX-02：识别答案普通模式

普通模式不得要求用户理解 `value` 和 `confidence` 数字。保存前后必须用自然语言显示当前选择及其用途；`source note` 必须明确是管理员私密备注。

### P0-UX-03：验证失败反馈

错误验证必须返回可见错误状态、剩余次数、冷却时间和锁定后的处理方式；失败不能表现成“什么都没发生”。

### P1-UI-01：交付主题和内容呈现

管理员可以为每个朋友配置封面、署名、开场文案、主题色、内容类型和内容顺序。未验证前不能泄露这些内容会指向谁；验证成功后再展示个性化内容。

### P1-UI-02：状态语义统一

所有状态使用同一套颜色、图标和中文词汇：完成、待处理、警告、阻塞、已停用、已归档、已撤销。不能出现“绿色的未完成”或中文和英文同时描述同一个状态。

### P1-UI-03：面向手机的朋友视角预览

后台在保存前提供朋友手机视角预览，至少覆盖首页、识别问题、猜测、最终验证、验证失败和内容解锁页面。

## 第二轮复测结论

| 需求 | 复测结果 |
|---|---|
| 首次配置向导 | 已出现，且说明了立即交付语义 |
| 真实后台统计 | 已改善 |
| 人物改名/状态修改 | 已通过 UI 验证 |
| 遗产内容重命名 | 已出现编辑入口 |
| Verification 启停 | 已出现启停入口 |
| 前台流程进度和立即交付提示 | 已出现 |
| 人物删除 | 仍不支持 |
| value/confidence/source note 易懂性 | 仍不通过 |
| 最终验证错误提示 | 仍不通过，用户看不到失败结果 |
| UI 的个人表达和完成度 | 仍需专项设计 |

因此，首轮 P0/P1 优化有效，但你提出的四个问题中，只有“编辑和状态管理”得到明显改善；删除语义、字段可理解性、错误反馈和视觉品质仍应作为下一轮优先事项。

## 第三轮复测：第二轮优化后的结果

复测重点：撤销交付/归档/永久删除、普通识别答案、验证失败反馈、前台表达配置和朋友手机视角预览。

### 已验证通过

#### 撤销交付和恢复

在朋友 A 详情页点击“撤销交付”后，页面状态变为：

- 人物状态：启用；
- 交付：已撤销；
- 操作按钮：恢复交付；
- 说明：重新允许公开识别和领取。

随后点击“恢复交付”，状态恢复为“可交付”。按钮、状态和解释已经能区分“人物是否启用”和“是否允许交付”，这比单纯的启用/停用清楚很多。

测试人物 B 仍显示为“归档”，列表也能区分启用、停用和归档。

#### 永久删除确认

永久删除现在以折叠区域呈现，展开后显示：

- 会删除人物、识别答案、专属验证、下载授权、交付配置和遗产密文文件；
- 审计只保留“已执行删除”事件，不保留信件原文或答案；
- 必须输入 `永久删除`；
- 提交按钮为“永久删除人物”。

删除确认机制已出现。本轮没有提交，因为这会真实删除测试人物，且需要动作时确认。下一步如要验证真正删除结果，应明确指定要删除的测试人物，并确认允许执行不可恢复操作。

#### 普通模式识别答案

普通模式已经改为：

- 是 / 大概是 / 不知道 / 大概不是 / 不是；
- 我很确定 / 一般确定 / 不太确定；
- 私密备注（可选），并有“例如：来自一次共同经历”的提示。

高级设置默认折叠，展开后才看到 `value` 和 `confidence` 数字字段。这个改动已经解决了“普通用户一上来不知道该填什么数字”的主要问题。

仍建议把“私密备注”的用途再做得更突出，例如在输入框旁直接显示“仅管理员可见，不会交付给朋友”。当前解释已经存在，但用户需要阅读整段说明才能理解。

#### 最终验证错误反馈

提交错误的最终验证答案后，前台现在显示：

> 答案不匹配，请检查拼写后重试。
>
> 还可尝试 4 次。请等待 1 秒后再试。

同时输入框和提交按钮在冷却期间被禁用。这个问题已经从“像按钮没反应”变成了可理解的验证流程，达到可用要求。

#### 主题、封面标题、开场文案、署名和内容类型

在朋友 A 详情页测试保存了主题、内容类型、封面标题、开场文案和署名；随后进入“朋友手机视角预览”，预览内容正确反映了测试值。测试结束后已恢复原来的默认表达。

预览页明确说明：

- 不创建正式会话；
- 不发放下载授权；
- 不写入正式审计；
- 下载按钮为禁用状态。

隔离预览的安全语义清楚，且确实能帮助管理员检查最终文案。

### 仍需改进

#### “朋友手机视角”还不是真正的手机预览

在 390px 手机宽度下，内容卡片本身可以响应式排列，但页面仍显示完整的管理员标题、退出登录按钮和后台导航。用户看到的是“后台中的手机内容预览”，不是朋友真正会看到的前台页面。

建议：

- 预览区域只保留朋友前台页面，不显示管理员导航和退出登录；
- 用真实前台模板渲染，而不是复制一份后台内容卡片；
- 提供桌面/手机切换按钮和明确的设备边框；
- 预览顶部保留“隔离预览”标记，但不要让后台 chrome 混入最终画面；
- 同时预览验证成功、下载中、下载完成和错误状态。

#### 主题目前更像“下拉选项”，还不是完整视觉系统

当前可以选“安静留白 / 温暖手写 / 深夜星光”，但内容结构、封面、排版和按钮样式变化还比较有限。对于“我不希望朋友觉得我很潦草”的需求，建议每个主题至少定义：

- 标题字体和正文排版；
- 背景纹理或色彩；
- 内容卡片样式；
- 下载按钮样式；
- 移动端间距和字号；
- 适合信件、照片、视频的不同布局。

主题不应只是换一个 CSS class，而应让管理员在预览中感到“这确实是我为朋友准备的交付页面”。

#### 状态仍有残留的语义矛盾

朋友 A 页面同时出现“身份答案已补齐”和 `Identity model incomplete`。前者表示 Trait 已填写，后者表示 Identity Integrity 尚未通过或尚未重算。对用户来说仍像两个相互冲突的结论。

建议拆成两个独立徽章：

- 身份答案：已补齐；
- 身份区分性：待重算 / 有风险 / 已通过。

不要再使用一个笼统的 `Identity model incomplete` 覆盖多种不同原因。

## 第三轮验收结论

| 项目 | 结果 |
|---|---|
| 撤销交付 | 通过，已验证撤销与恢复 |
| 归档 | 通过，测试人物保持归档 |
| 永久删除确认界面 | 通过，实际删除待动作确认 |
| 普通模式识别答案 | 通过 |
| 高级数字字段折叠 | 通过 |
| 验证失败提示 | 通过 |
| 剩余次数和冷却状态 | 通过 |
| 主题/封面/开场文案/署名/内容类型 | 通过保存和预览 |
| 隔离预览不创建授权 | 通过页面语义验证 |
| 真正的手机前台预览 | 不完整，仍包含后台导航 |
| UI 个性化完成度 | 有入口，但主题视觉差异仍有限 |

本轮第二轮优化已经解决了此前最影响可用性的三个问题：字段难懂、验证失败无反馈、无法区分撤销/归档。当前最值得继续推进的是：完成一次真实的永久删除验收、把手机预览改成真正的前台视图，以及把主题配置做成有明显质感的视觉系统。
