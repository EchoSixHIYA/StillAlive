services:
  still-alive:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - still_alive_data:/app/data
    restart: unless-stopped

volumes:
  still_alive_data:

